{
  "buildCommand": "pip install -r api/requirements.txt",
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python@3.1",
      "config": {
        "maxLambdaSize": "50mb",
        "requestsCacheSize": "max"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ],
  "env": {
    "ENVIRONMENT": "production",
    "DATABASE_URL": "sqlite:///:memory:"
  }
}
